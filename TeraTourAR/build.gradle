// GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN

buildscript {
    ext.kotlin_version = '1.3.10'
    repositories {
        jcenter()
        google()
        maven { url "https://jitpack.io" }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        flatDir {
            dirs 'libs'
        }
        jcenter()

        maven {
            url 'https://dl.bintray.com/kotlin/kotlin-dev/'
        }
        google()

    }
}

apply plugin: 'com.android.library'

dependencies {
    def lifecycle_version = "1.1.1"
    def support_version = "28.0.0"
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    api "com.android.support:appcompat-v7:$support_version"
    api "com.android.support:design:$support_version"
    api "com.android.support:support-v4:$support_version"
    api "com.android.support:cardview-v7:$support_version"
    api "com.android.support:recyclerview-v7:$support_version"
    api 'com.android.support.constraint:constraint-layout:1.1.3'
    // ViewModel and LiveData
    api "android.arch.lifecycle:extensions:$lifecycle_version"
    //     Support library depends on this lightweight import
    api "android.arch.lifecycle:runtime:$lifecycle_version"
    // alternately - if using Java8, use the following instead of compiler
    api "android.arch.lifecycle:common-java8:$lifecycle_version"
    //    api(name: 'android_component', ext: 'aar')
    //    api(name: 'core-1.1.0', ext: 'aar')
    //    api(name: 'unity_android_bridge', ext: 'aar')
    //    implementation(name: 'teratourandroidlib-release', ext: 'aar')
    //implementation project(':teratourandroidlib')
    //implementation project(':everyplay')
    implementation files('libs/android_component.aar')
    implementation files('libs/arcore_client.aar')
    implementation files('libs/ARExtensions.aar')
    implementation files('libs/unity_android_bridge.aar')
    implementation files('libs/unityandroidpermissions.aar')
    implementation files('libs/UnityARCore.aar')
    api project(':everyplay')
    api project(':teratourandroidlib')
}

//configurations.all {
//    resolutionStrategy {
//        force 'com.android.support:support-v4:28.0.0'
//    }}

android {
    compileSdkVersion 28

    dataBinding{
        enabled = true
    }

    defaultConfig {
//        applicationId "com.itera.teratour"
        minSdkVersion 19
        ndk {
            abiFilters 'armeabi-v7a', 'x86'
        }
        versionCode 1
        versionName '1.0'
    }

    lintOptions {
        abortOnError false
    }

    aaptOptions {
        noCompress '.unity3d', '.ress', '.resource', '.obb', 'wikitude/car.wtc', 'wikitude/dinosaurs.wtc', 'wikitude/firetruck.wto', 'wikitude/iot_tracker.wtc', 'wikitude/magazine.wtc'
        ignoreAssetsPattern = "!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~"
    }

    buildTypes {
        debug {
            minifyEnabled false
            useProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            signingConfig signingConfigs.debug
            jniDebuggable true
        }
        release {
            minifyEnabled true
            useProguard true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-unity.txt'
            signingConfig signingConfigs.debug
        }
    }

    packagingOptions {
        doNotStrip '*/armeabi-v7a/*.so'
        doNotStrip '*/x86/*.so'
        exclude 'META-INF/DEPENDENCIES'
        exclude 'META-INF/LICENSE'
        exclude 'META-INF/LICENSE.txt'
        exclude 'META-INF/license.txt'
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/NOTICE.txt'
        exclude 'META-INF/notice.txt'
        exclude 'META-INF/ASL2.0'
        exclude 'META-INF/proguard/androidx-annotations.pro'
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}
